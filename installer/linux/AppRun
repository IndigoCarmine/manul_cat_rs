#!/bin/sh
# AppImage entrypoint. Resolves the mounted AppDir at runtime and hands off to
# the real binary, forwarding any arguments (e.g. a file the user dropped on it).
HERE="$(dirname "$(readlink -f "$0")")"
exec "${HERE}/usr/bin/manul_cat_rs" "$@"
